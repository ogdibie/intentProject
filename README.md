# intentProject
A simple project to calculate product cost based on a given criteria in the Question. The main call contains the main method that was used in testing the app. 

To RUN it, navigate to the src directory and simple run javac Main.java TerminalProgam.java, then run java Main. It should display the outputs as required in the question.
